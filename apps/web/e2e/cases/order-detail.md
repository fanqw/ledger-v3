# 测试用例：订单明细管理（OrderDetail + CreatableSelect）

> 前置条件：已登录，存在至少一条订单，已进入订单详情页

## A. 订单详情页加载

- [x] A1. 页面显示订单名称（h1）
- [x] A2. 显示进货地、创建时间（如有）
- [x] A3. 显示「添加明细」按钮
- [x] A4. 如无明细，显示「暂无明细」空状态
- [x] A5. 如有明细，显示明细表格：分类、名称、数量、单位、单价、金额、备注、分类金额、总金额、操作
- [x] A6. 明细按分类分组，分类列和分类金额列正确合并（rowSpan）
- [x] A7. 分类小计和总计正确
- [x] A8. 总金额列跨所有明细行合并

## B. 添加明细 — 选择已有商品

- [x] B1. 点击「添加明细」，弹窗标题为「添加明细」
- [x] B2. 商品下拉框显示 placeholder「搜索选择商品...」（浅色）
- [x] B3. 点击商品下拉框，展开已有商品列表（API 数据正常加载）
- [x] B4. 选择一个商品，下拉框回显商品名称
- [x] B5. 输入数量 > 0 和单价 >= 0，金额自动计算（数量 × 单价）
- [x] B6. 修改数量或单价，金额重新计算
- [x] B7. 点击保存，弹窗关闭，明细列表更新

## C. 添加明细 — 即输即建（type-to-create）

- [x] C1. 商品下拉框输入不存在的名称，列表第一条显示输入值
- [x] C2. 点击新建项，商品下拉框回显该名称
- [x] C3. 分类下拉框输入新名称，同样可以新建
- [x] C4. 单位下拉框输入新名称，同样可以新建
- [x] C5. 三个下拉框都选择新建项后，下拉框各自回显正确
- [x] C6. 填写数量、单价，金额自动计算
- [x] C7. 点击保存，明细添加成功，商品/分类/单位在后台自动创建
- [x] C8. 明细列表中分类名称、商品名称、单位名称正确显示

## D. 新建项持久化（同一次弹窗内）

- [x] D1. 新建商品「测试A」→ 下拉框回显正确
- [x] D2. 关闭弹窗前：重新打开商品下拉框 → 已有数据 +「测试A」共存
- [x] D3. 输入不匹配的搜索关键词 →「测试A」隐藏（不匹配），API 项正常过滤
- [x] D4. 清空搜索 →「测试A」重新出现，API 全量数据恢复
- [x] D5. 搜索时 select 触发器值不丢失

## E. 搜索时选中值保持

- [x] E1. 新建商品后，商品下拉框显示新建项名称
- [x] E2. 重新打开下拉框，输入不匹配的关键词 → 下拉列表过滤，但 select 触发器值不变
- [x] E3. 清空搜索 → 列表恢复，触发器值不变

## F. 编辑明细

- [x] F1. 点击明细行的编辑按钮，弹窗标题为「编辑明细」
- [x] F2. 修改数量，金额重新计算
- [x] F3. 修改单价，金额重新计算
- [x] F4. 手动修改金额（使偏差 > 0.005），金额输入框显示红色边框
- [x] F5. 保存后，明细行金额列显示红色（lineTotal 被手动修改过）
- [x] F6. 即输即建创建的明细，编辑时分类和单位正确回显（不显示 placeholder）

## G. lineTotal 双向联动

- [x] G1. 修改数量或单价 → 金额自动计算，清除红色状态
- [x] G2. 手动修改金额（使 lineTotal ≠ 数量 × 单价）→ 输入框红色边框，单价反向计算
- [x] G3. 保存后，明细表格中该行金额显示红色字体
- [x] G4. 再次编辑该明细 → 金额输入框仍有红色边框

## H. 删除明细

- [x] H1. 点击明细行的删除按钮，弹出确认对话框
- [x] H2. 确认删除，明细从列表移除
- [x] H3. 取消删除，明细保持不变

## I. Excel 导出

- [x] I1. 点击「导出 Excel」，触发文件下载
- [x] I2. 导出的 Excel 包含：订单名称、进货地、创建时间
- [x] I3. 明细数据正确（包含表头、分组、合并单元格）
- [x] I4. 分类金额列和总金额列合并正确
- [x] I5. 手动修改过 lineTotal 的行，金额列显示红色
- [x] I6. 末尾有总计行（双线上边框）

## J. 编辑订单

- [x] J1. 点击「编辑」按钮（页面顶部），弹窗标题为「编辑订单」
- [x] J2. 修改订单名称，保存成功
- [x] J3. 修改进货地，保存成功
- [x] J4. 修改备注，保存成功

## K. 返回列表

- [x] K1. 点击返回箭头，导航到 /orders
- [x] K2. 订单列表正确显示

## L. 边界情况

- [x] L1. 商品下拉框搜索不存在的关键词 → 显示新建项
- [x] L2. 数量填 0 或负数 → 提示错误
- [x] L3. 单价填负数 → 提示错误
- [x] L4. 不选商品直接保存 → 提示「请选择或输入商品」
- [x] L5. 多个新建项按创建顺序排列（最新在最前）
- [x] L6. 新建项名称与已有项完全匹配 → 不显示新建选项

## M. 回归检查

- [x] M1. 全程无 JS 错误（Console 无 pageerror）
- [x] M2. 无 React key 重复警告
- [x] M3. API 请求正确携带 JWT Token
