## 1. 测试基线与主题基础

- [ ] 1.1 为应用主题 token、页面标题区和工具栏编写失败测试，覆盖品牌色、主操作位置与可访问名称
- [ ] 1.2 在应用级 ConfigProvider 与全局样式中实现颜色、字阶、背景、边框、圆角、阴影、焦点和减少动态效果 token
- [ ] 1.3 实现并测试 `PageHeader`、`PageToolbar` 和统一异步状态组件，确保组件不持有业务请求状态

## 2. 响应式数据组件

- [ ] 2.1 为桌面表格、平板列降级和手机摘要条目编写视口行为测试
- [ ] 2.2 实现 `ResponsiveDataView`，复用同一行数据并允许页面声明移动端摘要字段
- [ ] 2.3 为行级查看、编辑、删除菜单编写键盘与可访问名称测试并实现 `RowActions`

## 3. 应用壳与导航

- [ ] 3.1 为桌面默认窄侧栏、持久化展开状态、平板窄侧栏和手机抽屉导航编写失败测试
- [ ] 3.2 重构 AppShell、SideNav 与 TopBar，复用单一导航配置并实现三个响应式断点
- [ ] 3.3 验证导航选中、面包屑、用户菜单、登出、键盘焦点和 `prefers-reduced-motion` 行为

## 4. 订单页面试点

- [ ] 4.1 为订单列表的统一页面层级、标题区新增入口、响应式摘要和重复提交防护补充测试
- [ ] 4.2 迁移订单列表到共享页面组件，移除 FloatButton，并保持搜索、分页、创建、编辑、删除和详情跳转逻辑不变
- [ ] 4.3 为订单详情的桌面、平板与手机布局补充测试，并实现关键列固定、受控横向滚动及操作入口可见

## 5. 基础资料页面

- [ ] 5.1 为分类、单位、商品和进货地页面补充共享结构与响应式行为测试
- [ ] 5.2 迁移分类与单位页面到统一标题、工具栏、数据状态、响应式列表和表单规范
- [ ] 5.3 迁移商品与进货地页面到统一规范，并保持关联选项、搜索、分页和 CRUD 行为不变

## 6. 数据分析工作台

- [ ] 6.1 为分析页筛选区、KPI 网格、图表状态和三个视口布局补充测试
- [ ] 6.2 迁移分析页到统一页面层级和主题 token，保留 ECharts 配置、数据请求和时间筛选语义
- [ ] 6.3 验证图表在容器与视口变化后正确 resize，且加载、空数据和失败状态可恢复

## 7. 全局回归与视觉验收

- [ ] 7.1 运行 `pnpm --filter web lint`、`pnpm --filter web test` 与 `pnpm --filter web build` 并修复失败
- [ ] 7.2 运行现有 `pnpm --filter web smoke:e2e` 与 `pnpm --filter web verify:e2e`，确认业务链路不回归
- [ ] 7.3 在桌面、平板和手机视口检查导航、代表性列表、订单详情和分析页，保存对照截图并修复溢出、遮挡与焦点问题
- [ ] 7.4 核对 responsive-data-workspace 与 workspace-navigation 的全部场景均有自动化断言或明确的视觉验收记录
